{
  "name": "organia-fertilizantes",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "dev": "nodemon -e html,css,js --exec \"http-server . -p 3000 --ssl --cert ./localhost.pem --key ./localhost-key.pem\"",
    "start": "http-server . -p 3000 --ssl --cert ./localhost.pem --key ./localhost-key.pem",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "nodemon": "^3.1.0"
  },
  "type": "module"
}
